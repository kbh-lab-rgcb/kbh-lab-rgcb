# Alumni photos

Optional. Name each photo to match the text file in [`../text/`](../text):

```text
photos/01-yadu-vijayan.jpg
text/01-yadu-vijayan.txt
```

Without a photo the person's initials are shown instead, which is perfectly fine
for older alumni.

Accepted: `.jpg` `.jpeg` `.png` `.webp` `.svg`
